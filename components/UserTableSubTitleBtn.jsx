import React from 'react';
import {
  View,
  Text,
  Button,
} from 'react-native';

const UserTableSubTitleBtn = ({ ttl }) => {
  return (<Button title={ttl} />);
};

export default UserTableSubTitleBtn;
